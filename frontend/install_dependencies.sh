#!/bin/bash

# Install React and React DOM
npm install react react-dom

# Install Material UI
npm install @mui/material @emotion/react @emotion/styled

# Install D3 for data visualization
npm install d3

# Install Axios for HTTP requests (used in MetadataRetriever.js)
npm install axios

# Install React Google Charts (used in SubjectDistribution.js)
npm install react-google-charts

# Install React Router (optional, if routing is needed)
npm install react-router-dom

# Install Flask (for the backend)
pip3 install Flask requests

for icons
npm install @mui/icons-material